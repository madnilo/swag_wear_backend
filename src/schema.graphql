# import * from './generated/prisma.graphql'

 
# Inputs
# Create custom inputs types as/if needed


# Mutations
# Mutations that are exposed to my javascript code
type Mutation {
    createItem(data: ItemUpdateInput): Item
}


# Queries
# Queries that are exposed to my javascript code
type Query {
    items: [Item]!
}